"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from openapi.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Any, Dict, List
from typing_extensions import NotRequired, TypedDict


class RuleResponseTypedDict(TypedDict):
    r"""Automation rule response."""

    id: str
    org_id: Nullable[str]
    creator_id: str
    name: str
    description: Nullable[str]
    scope: str
    scope_id: Nullable[str]
    enabled: bool
    priority: int
    conditions: Dict[str, Any]
    actions: List[Dict[str, Any]]
    schedule: Dict[str, Any]
    constraints: Nullable[Dict[str, Any]]
    rule_metadata: Nullable[Dict[str, Any]]
    version: int
    created_at: datetime
    updated_at: datetime
    scope_mode: NotRequired[Nullable[str]]


class RuleResponse(BaseModel):
    r"""Automation rule response."""

    id: str

    org_id: Nullable[str]

    creator_id: str

    name: str

    description: Nullable[str]

    scope: str

    scope_id: Nullable[str]

    enabled: bool

    priority: int

    conditions: Dict[str, Any]

    actions: List[Dict[str, Any]]

    schedule: Dict[str, Any]

    constraints: Nullable[Dict[str, Any]]

    rule_metadata: Nullable[Dict[str, Any]]

    version: int

    created_at: datetime

    updated_at: datetime

    scope_mode: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["scope_mode"])
        nullable_fields = set(
            [
                "org_id",
                "description",
                "scope_id",
                "constraints",
                "rule_metadata",
                "scope_mode",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
