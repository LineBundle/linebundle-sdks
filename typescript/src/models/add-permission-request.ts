/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type AddPermissionRequest = {
  /**
   * Permission code (e.g., 'events:create')
   */
  permissionCode: string;
};

/** @internal */
export type AddPermissionRequest$Outbound = {
  permission_code: string;
};

/** @internal */
export const AddPermissionRequest$outboundSchema: z.ZodMiniType<
  AddPermissionRequest$Outbound,
  AddPermissionRequest
> = z.pipe(
  z.object({
    permissionCode: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      permissionCode: "permission_code",
    });
  }),
);

export function addPermissionRequestToJSON(
  addPermissionRequest: AddPermissionRequest,
): string {
  return JSON.stringify(
    AddPermissionRequest$outboundSchema.parse(addPermissionRequest),
  );
}
