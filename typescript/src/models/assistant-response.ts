/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type AssistantResponse = {
  /**
   * Assistant ID
   */
  id: string;
  /**
   * Assistant username
   */
  username: string;
  /**
   * Whether this is an AI assistant
   */
  isAiAssistant: boolean;
  /**
   * Owner user ID
   */
  ownerId: string;
  /**
   * Chat room ID for conversations with the assistant
   */
  roomId?: string | null | undefined;
};

/** @internal */
export const AssistantResponse$inboundSchema: z.ZodMiniType<
  AssistantResponse,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    username: types.string(),
    is_ai_assistant: types.boolean(),
    owner_id: types.string(),
    room_id: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "is_ai_assistant": "isAiAssistant",
      "owner_id": "ownerId",
      "room_id": "roomId",
    });
  }),
);

export function assistantResponseFromJSON(
  jsonString: string,
): SafeParseResult<AssistantResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AssistantResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AssistantResponse' from JSON`,
  );
}
