/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type AvatarUploadResponse = {
  /**
   * Uploaded avatar URL
   */
  avatarUrl: string;
  /**
   * Success message
   */
  message: string;
};

/** @internal */
export const AvatarUploadResponse$inboundSchema: z.ZodMiniType<
  AvatarUploadResponse,
  unknown
> = z.pipe(
  z.object({
    avatar_url: types.string(),
    message: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "avatar_url": "avatarUrl",
    });
  }),
);

export function avatarUploadResponseFromJSON(
  jsonString: string,
): SafeParseResult<AvatarUploadResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AvatarUploadResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AvatarUploadResponse' from JSON`,
  );
}
