/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { ClosedEnum } from "../types/enums.js";

/**
 * Field to sort by. Available fields: id, title, description, created_dt, start_dt, end_dt, visibility, creator_id, is_deleted, is_locked, latitude, longitude
 */
export const Field = {
  Id: "id",
  Title: "title",
  Description: "description",
  CreatedDt: "created_dt",
  StartDt: "start_dt",
  EndDt: "end_dt",
  Visibility: "visibility",
  CreatorId: "creator_id",
  IsDeleted: "is_deleted",
  IsLocked: "is_locked",
  Latitude: "latitude",
  Longitude: "longitude",
} as const;
/**
 * Field to sort by. Available fields: id, title, description, created_dt, start_dt, end_dt, visibility, creator_id, is_deleted, is_locked, latitude, longitude
 */
export type Field = ClosedEnum<typeof Field>;

/**
 * Sort order: 'asc' for ascending, 'desc' for descending
 */
export const Order = {
  Asc: "asc",
  Desc: "desc",
} as const;
/**
 * Sort order: 'asc' for ascending, 'desc' for descending
 */
export type Order = ClosedEnum<typeof Order>;

/**
 * Individual sort field configuration.
 */
export type EventSortFieldConfig = {
  /**
   * Field to sort by. Available fields: id, title, description, created_dt, start_dt, end_dt, visibility, creator_id, is_deleted, is_locked, latitude, longitude
   */
  field: Field;
  /**
   * Sort order: 'asc' for ascending, 'desc' for descending
   */
  order?: Order | undefined;
};

/** @internal */
export const Field$outboundSchema: z.ZodMiniEnum<typeof Field> = z.enum(Field);

/** @internal */
export const Order$outboundSchema: z.ZodMiniEnum<typeof Order> = z.enum(Order);

/** @internal */
export type EventSortFieldConfig$Outbound = {
  field: string;
  order: string;
};

/** @internal */
export const EventSortFieldConfig$outboundSchema: z.ZodMiniType<
  EventSortFieldConfig$Outbound,
  EventSortFieldConfig
> = z.object({
  field: Field$outboundSchema,
  order: z._default(Order$outboundSchema, "asc"),
});

export function eventSortFieldConfigToJSON(
  eventSortFieldConfig: EventSortFieldConfig,
): string {
  return JSON.stringify(
    EventSortFieldConfig$outboundSchema.parse(eventSortFieldConfig),
  );
}
