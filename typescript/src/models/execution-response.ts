/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Rule execution response.
 */
export type ExecutionResponse = {
  id: string;
  ruleId: string;
  orgId: string | null;
  creatorId: string;
  executionKey: string;
  triggerType: string;
  triggerContext: { [k: string]: any } | null;
  status: string;
  startedAt: Date | null;
  completedAt: Date | null;
  conditionResults: { [k: string]: any } | null;
  actionResults: { [k: string]: any } | null;
  errorMessage: string | null;
  retryCount: number;
  createdAt: Date;
};

/** @internal */
export const ExecutionResponse$inboundSchema: z.ZodMiniType<
  ExecutionResponse,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    rule_id: types.string(),
    org_id: types.nullable(types.string()),
    creator_id: types.string(),
    execution_key: types.string(),
    trigger_type: types.string(),
    trigger_context: types.nullable(z.record(z.string(), z.any())),
    status: types.string(),
    started_at: types.nullable(types.date()),
    completed_at: types.nullable(types.date()),
    condition_results: types.nullable(z.record(z.string(), z.any())),
    action_results: types.nullable(z.record(z.string(), z.any())),
    error_message: types.nullable(types.string()),
    retry_count: types.number(),
    created_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "rule_id": "ruleId",
      "org_id": "orgId",
      "creator_id": "creatorId",
      "execution_key": "executionKey",
      "trigger_type": "triggerType",
      "trigger_context": "triggerContext",
      "started_at": "startedAt",
      "completed_at": "completedAt",
      "condition_results": "conditionResults",
      "action_results": "actionResults",
      "error_message": "errorMessage",
      "retry_count": "retryCount",
      "created_at": "createdAt",
    });
  }),
);

export function executionResponseFromJSON(
  jsonString: string,
): SafeParseResult<ExecutionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExecutionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExecutionResponse' from JSON`,
  );
}
