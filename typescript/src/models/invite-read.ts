/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Schema representing an organization invitation.
 */
export type InviteRead = {
  /**
   * Invitation ID
   */
  id: string;
  /**
   * Organization ID
   */
  orgId: string;
  /**
   * Invitee email address
   */
  email: string;
  /**
   * Role to assign on acceptance
   */
  role: string;
  /**
   * Invitation status
   */
  status: string;
  /**
   * ID of user who sent invite
   */
  inviterId: string;
  /**
   * When invitation expires
   */
  expiresAt?: Date | null | undefined;
  /**
   * When invitation was created
   */
  createdAt?: Date | null | undefined;
};

/** @internal */
export const InviteRead$inboundSchema: z.ZodMiniType<InviteRead, unknown> = z
  .pipe(
    z.object({
      id: types.string(),
      org_id: types.string(),
      email: types.string(),
      role: z._default(types.string(), "member"),
      status: z._default(types.string(), "pending"),
      inviter_id: types.string(),
      expires_at: z.optional(z.nullable(types.date())),
      created_at: z.optional(z.nullable(types.date())),
    }),
    z.transform((v) => {
      return remap$(v, {
        "org_id": "orgId",
        "inviter_id": "inviterId",
        "expires_at": "expiresAt",
        "created_at": "createdAt",
      });
    }),
  );

export function inviteReadFromJSON(
  jsonString: string,
): SafeParseResult<InviteRead, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InviteRead$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InviteRead' from JSON`,
  );
}
