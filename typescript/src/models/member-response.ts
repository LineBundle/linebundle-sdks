/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type MemberResponse = {
  id: string;
  roomId: string;
  userId: string;
  role: string;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const MemberResponse$inboundSchema: z.ZodMiniType<
  MemberResponse,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    room_id: types.string(),
    user_id: types.string(),
    role: types.string(),
    created_at: types.date(),
    updated_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "room_id": "roomId",
      "user_id": "userId",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function memberResponseFromJSON(
  jsonString: string,
): SafeParseResult<MemberResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MemberResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MemberResponse' from JSON`,
  );
}
