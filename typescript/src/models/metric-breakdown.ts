/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type MetricBreakdown = {
  total: number;
  assigned: number;
  unassigned: number;
};

/** @internal */
export const MetricBreakdown$inboundSchema: z.ZodMiniType<
  MetricBreakdown,
  unknown
> = z.object({
  total: z._default(types.number(), 0),
  assigned: z._default(types.number(), 0),
  unassigned: z._default(types.number(), 0),
});

export function metricBreakdownFromJSON(
  jsonString: string,
): SafeParseResult<MetricBreakdown, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricBreakdown$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricBreakdown' from JSON`,
  );
}
