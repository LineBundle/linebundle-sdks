/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type OrganizationRead = {
  id: string;
  plan: string;
  allowPublicVisibility: boolean;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const OrganizationRead$inboundSchema: z.ZodMiniType<
  OrganizationRead,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    plan: types.string(),
    allow_public_visibility: z._default(types.boolean(), false),
    created_at: types.date(),
    updated_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "allow_public_visibility": "allowPublicVisibility",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function organizationReadFromJSON(
  jsonString: string,
): SafeParseResult<OrganizationRead, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OrganizationRead$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OrganizationRead' from JSON`,
  );
}
