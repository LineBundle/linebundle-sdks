/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  GetSpaceListResponseDTO,
  GetSpaceListResponseDTO$inboundSchema,
} from "./get-space-list-response-dto.js";

/**
 * Paginated response for space list to support infinite scrolling
 */
export type PaginatedSpaceListResponseDTO = {
  /**
   * List of spaces
   */
  items: Array<GetSpaceListResponseDTO>;
  /**
   * Whether there are more items available
   */
  hasNext: boolean;
  /**
   * Whether there are previous items available
   */
  hasPrev: boolean;
  /**
   * Total number of spaces matching the query
   */
  totalCount: number;
  /**
   * Cursor for next page (space ID)
   */
  nextCursor?: number | null | undefined;
  /**
   * Cursor for previous page (space ID)
   */
  prevCursor?: number | null | undefined;
};

/** @internal */
export const PaginatedSpaceListResponseDTO$inboundSchema: z.ZodMiniType<
  PaginatedSpaceListResponseDTO,
  unknown
> = z.pipe(
  z.object({
    items: z.array(GetSpaceListResponseDTO$inboundSchema),
    has_next: types.boolean(),
    has_prev: types.boolean(),
    total_count: types.number(),
    next_cursor: z.optional(z.nullable(types.number())),
    prev_cursor: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "has_next": "hasNext",
      "has_prev": "hasPrev",
      "total_count": "totalCount",
      "next_cursor": "nextCursor",
      "prev_cursor": "prevCursor",
    });
  }),
);

export function paginatedSpaceListResponseDTOFromJSON(
  jsonString: string,
): SafeParseResult<PaginatedSpaceListResponseDTO, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaginatedSpaceListResponseDTO$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaginatedSpaceListResponseDTO' from JSON`,
  );
}
