/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type PlaceAnalyticTotal = {
  totalCount: number;
};

/** @internal */
export const PlaceAnalyticTotal$inboundSchema: z.ZodMiniType<
  PlaceAnalyticTotal,
  unknown
> = z.pipe(
  z.object({
    total_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "total_count": "totalCount",
    });
  }),
);

export function placeAnalyticTotalFromJSON(
  jsonString: string,
): SafeParseResult<PlaceAnalyticTotal, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlaceAnalyticTotal$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlaceAnalyticTotal' from JSON`,
  );
}
