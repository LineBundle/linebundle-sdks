/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  MetricBreakdown,
  MetricBreakdown$inboundSchema,
} from "./metric-breakdown.js";

export type PulseDTO = {
  members: MetricBreakdown;
  events: MetricBreakdown;
  materials: MetricBreakdown;
  /**
   * 'growth', 'stable', 'quiet'
   */
  velocity: string;
};

/** @internal */
export const PulseDTO$inboundSchema: z.ZodMiniType<PulseDTO, unknown> = z
  .object({
    members: MetricBreakdown$inboundSchema,
    events: MetricBreakdown$inboundSchema,
    materials: MetricBreakdown$inboundSchema,
    velocity: z._default(types.string(), "stable"),
  });

export function pulseDTOFromJSON(
  jsonString: string,
): SafeParseResult<PulseDTO, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PulseDTO$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PulseDTO' from JSON`,
  );
}
