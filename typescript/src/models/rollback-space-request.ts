/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * Request to rollback space to a specific version.
 */
export type RollbackSpaceRequest = {
  /**
   * Version number to rollback to
   */
  versionNumber: number;
};

/** @internal */
export type RollbackSpaceRequest$Outbound = {
  version_number: number;
};

/** @internal */
export const RollbackSpaceRequest$outboundSchema: z.ZodMiniType<
  RollbackSpaceRequest$Outbound,
  RollbackSpaceRequest
> = z.pipe(
  z.object({
    versionNumber: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      versionNumber: "version_number",
    });
  }),
);

export function rollbackSpaceRequestToJSON(
  rollbackSpaceRequest: RollbackSpaceRequest,
): string {
  return JSON.stringify(
    RollbackSpaceRequest$outboundSchema.parse(rollbackSpaceRequest),
  );
}
