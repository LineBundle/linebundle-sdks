/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Partial response model for Content (Description, Metadata, Label).
 *
 * @remarks
 * Used by `PATCH /content` and `GET /section/content`.
 */
export type SectionContentRead = {
  /**
   * Event ID
   */
  id: number;
  /**
   * Top-level event title
   */
  title: string;
  /**
   * Event status
   */
  status?: string | null | undefined;
  /**
   * Description
   */
  description?: string | null | undefined;
  /**
   * Metadata
   */
  eventMetadata?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const SectionContentRead$inboundSchema: z.ZodMiniType<
  SectionContentRead,
  unknown
> = z.pipe(
  z.object({
    id: types.number(),
    title: types.string(),
    status: z.optional(z.nullable(types.string())),
    description: z.optional(z.nullable(types.string())),
    event_metadata: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "event_metadata": "eventMetadata",
    });
  }),
);

export function sectionContentReadFromJSON(
  jsonString: string,
): SafeParseResult<SectionContentRead, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SectionContentRead$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SectionContentRead' from JSON`,
  );
}
