/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SpaceContactRead,
  SpaceContactRead$inboundSchema,
} from "./space-contact-read.js";

/**
 * Model for list of space contacts with contact info.
 */
export type SpaceContactList = {
  /**
   * List of space contacts
   */
  contacts?: Array<SpaceContactRead> | undefined;
  /**
   * Total number of contacts
   */
  total: number;
};

/** @internal */
export const SpaceContactList$inboundSchema: z.ZodMiniType<
  SpaceContactList,
  unknown
> = z.object({
  contacts: types.optional(z.array(SpaceContactRead$inboundSchema)),
  total: types.number(),
});

export function spaceContactListFromJSON(
  jsonString: string,
): SafeParseResult<SpaceContactList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceContactList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceContactList' from JSON`,
  );
}
