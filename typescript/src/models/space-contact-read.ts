/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  SpaceContactInfo,
  SpaceContactInfo$inboundSchema,
} from "./space-contact-info.js";

/**
 * Pydantic model for reading space contact data.
 */
export type SpaceContactRead = {
  /**
   * Space contact relationship ID
   */
  id: number;
  /**
   * Space ID
   */
  spaceId: number;
  /**
   * Contact ID
   */
  contactId: string;
  /**
   * Creation timestamp
   */
  createdAt: string;
  /**
   * Last update timestamp
   */
  updatedAt: string;
  /**
   * Contact information
   */
  contact?: SpaceContactInfo | null | undefined;
};

/** @internal */
export const SpaceContactRead$inboundSchema: z.ZodMiniType<
  SpaceContactRead,
  unknown
> = z.pipe(
  z.object({
    id: types.number(),
    space_id: types.number(),
    contact_id: types.string(),
    created_at: types.string(),
    updated_at: types.string(),
    contact: z.optional(z.nullable(SpaceContactInfo$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "space_id": "spaceId",
      "contact_id": "contactId",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function spaceContactReadFromJSON(
  jsonString: string,
): SafeParseResult<SpaceContactRead, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceContactRead$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceContactRead' from JSON`,
  );
}
