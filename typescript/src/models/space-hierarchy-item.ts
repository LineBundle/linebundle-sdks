/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Space hierarchy item for tree display
 */
export type SpaceHierarchyItem = {
  id: number;
  title: string;
  description: string;
  parentId: number | null;
  children?: Array<SpaceHierarchyItem> | undefined;
  level: number;
  isInherited: boolean;
};

/** @internal */
export const SpaceHierarchyItem$inboundSchema: z.ZodMiniType<
  SpaceHierarchyItem,
  unknown
> = z.pipe(
  z.object({
    id: types.number(),
    title: types.string(),
    description: types.string(),
    parent_id: types.nullable(types.number()),
    children: types.optional(
      z.array(z.lazy(() => SpaceHierarchyItem$inboundSchema)),
    ),
    level: z._default(types.number(), 0),
    is_inherited: z._default(types.boolean(), true),
  }),
  z.transform((v) => {
    return remap$(v, {
      "parent_id": "parentId",
      "is_inherited": "isInherited",
    });
  }),
);

export function spaceHierarchyItemFromJSON(
  jsonString: string,
): SafeParseResult<SpaceHierarchyItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceHierarchyItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceHierarchyItem' from JSON`,
  );
}
