/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Space inheritance configuration
 */
export type SpaceInheritanceSettings = {
  inheritVisibility: boolean;
  inheritMembers: boolean;
  inheritPermissions: boolean;
  inheritTimezone: boolean;
};

/** @internal */
export const SpaceInheritanceSettings$inboundSchema: z.ZodMiniType<
  SpaceInheritanceSettings,
  unknown
> = z.pipe(
  z.object({
    inherit_visibility: z._default(types.boolean(), true),
    inherit_members: z._default(types.boolean(), true),
    inherit_permissions: z._default(types.boolean(), true),
    inherit_timezone: z._default(types.boolean(), true),
  }),
  z.transform((v) => {
    return remap$(v, {
      "inherit_visibility": "inheritVisibility",
      "inherit_members": "inheritMembers",
      "inherit_permissions": "inheritPermissions",
      "inherit_timezone": "inheritTimezone",
    });
  }),
);

export function spaceInheritanceSettingsFromJSON(
  jsonString: string,
): SafeParseResult<SpaceInheritanceSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceInheritanceSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceInheritanceSettings' from JSON`,
  );
}
