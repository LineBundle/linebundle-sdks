/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Space role read model for API responses
 */
export type SpaceRoleRead = {
  id: string;
  spaceId: number;
  name: string;
  description?: string | null | undefined;
  orgId: string;
  permissions?: Array<any> | undefined;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const SpaceRoleRead$inboundSchema: z.ZodMiniType<
  SpaceRoleRead,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    space_id: types.number(),
    name: types.string(),
    description: z.optional(z.nullable(types.string())),
    org_id: types.string(),
    permissions: types.optional(z.array(z.any())),
    created_at: types.date(),
    updated_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "space_id": "spaceId",
      "org_id": "orgId",
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function spaceRoleReadFromJSON(
  jsonString: string,
): SafeParseResult<SpaceRoleRead, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceRoleRead$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceRoleRead' from JSON`,
  );
}
