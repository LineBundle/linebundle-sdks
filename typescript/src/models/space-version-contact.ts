/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  ContactResponse,
  ContactResponse$inboundSchema,
} from "./contact-response.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type SpaceVersionContact = {
  id: number;
  contactId: number;
  contact: ContactResponse | null;
};

/** @internal */
export const SpaceVersionContact$inboundSchema: z.ZodMiniType<
  SpaceVersionContact,
  unknown
> = z.pipe(
  z.object({
    id: types.number(),
    contact_id: types.number(),
    contact: types.nullable(ContactResponse$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "contact_id": "contactId",
    });
  }),
);

export function spaceVersionContactFromJSON(
  jsonString: string,
): SafeParseResult<SpaceVersionContact, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceVersionContact$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceVersionContact' from JSON`,
  );
}
