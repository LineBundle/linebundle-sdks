/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type SpaceVersionProfile = {
  id: string;
  email: string | null;
  firstName: string | null;
  lastName: string | null;
  avatarUrl: string | null;
};

/** @internal */
export const SpaceVersionProfile$inboundSchema: z.ZodMiniType<
  SpaceVersionProfile,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    email: types.nullable(types.string()),
    first_name: types.nullable(types.string()),
    last_name: types.nullable(types.string()),
    avatar_url: types.nullable(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "first_name": "firstName",
      "last_name": "lastName",
      "avatar_url": "avatarUrl",
    });
  }),
);

export function spaceVersionProfileFromJSON(
  jsonString: string,
): SafeParseResult<SpaceVersionProfile, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SpaceVersionProfile$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SpaceVersionProfile' from JSON`,
  );
}
