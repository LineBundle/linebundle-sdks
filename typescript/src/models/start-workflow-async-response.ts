/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type StartWorkflowAsyncResponse = {
  /**
   * Workflow execution ID
   */
  executionId: string;
  /**
   * Celery task ID
   */
  taskId: string;
  /**
   * Initial status (pending)
   */
  status: string;
  /**
   * Status message
   */
  message: string;
};

/** @internal */
export const StartWorkflowAsyncResponse$inboundSchema: z.ZodMiniType<
  StartWorkflowAsyncResponse,
  unknown
> = z.pipe(
  z.object({
    execution_id: types.string(),
    task_id: types.string(),
    status: types.string(),
    message: z._default(types.string(), "Workflow execution queued"),
  }),
  z.transform((v) => {
    return remap$(v, {
      "execution_id": "executionId",
      "task_id": "taskId",
    });
  }),
);

export function startWorkflowAsyncResponseFromJSON(
  jsonString: string,
): SafeParseResult<StartWorkflowAsyncResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StartWorkflowAsyncResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StartWorkflowAsyncResponse' from JSON`,
  );
}
