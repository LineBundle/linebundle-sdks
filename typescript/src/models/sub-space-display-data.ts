/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Display data for sub-space in list
 */
export type SubSpaceDisplayData = {
  id: number;
  title: string;
  description: string;
  eventCount: number;
  memberCount: number;
  isInherited: boolean;
};

/** @internal */
export const SubSpaceDisplayData$inboundSchema: z.ZodMiniType<
  SubSpaceDisplayData,
  unknown
> = z.pipe(
  z.object({
    id: types.number(),
    title: types.string(),
    description: types.string(),
    event_count: z._default(types.number(), 0),
    member_count: z._default(types.number(), 0),
    is_inherited: z._default(types.boolean(), true),
  }),
  z.transform((v) => {
    return remap$(v, {
      "event_count": "eventCount",
      "member_count": "memberCount",
      "is_inherited": "isInherited",
    });
  }),
);

export function subSpaceDisplayDataFromJSON(
  jsonString: string,
): SafeParseResult<SubSpaceDisplayData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubSpaceDisplayData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubSpaceDisplayData' from JSON`,
  );
}
