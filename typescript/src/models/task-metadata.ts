/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Task category
 */
export const Category = {
  Trigger: "trigger",
  Condition: "condition",
  Action: "action",
} as const;
/**
 * Task category
 */
export type Category = OpenEnum<typeof Category>;

/**
 * Metadata describing an automation task (trigger, condition, or action).
 */
export type TaskMetadata = {
  /**
   * Unique task identifier (e.g., 'send_email_notification')
   */
  taskType: string;
  /**
   * Task category
   */
  category: Category;
  /**
   * Human-readable task name
   */
  name: string;
  /**
   * Task description for users
   */
  description: string;
  /**
   * Icon identifier (lucide-react icon name)
   */
  icon?: string | null | undefined;
  /**
   * Tags for categorization
   */
  tags?: Array<string> | undefined;
  /**
   * JSON Schema defining task configuration structure
   */
  configSchema: { [k: string]: any };
  /**
   * UI-specific configuration (layout hints, grouping, etc.)
   */
  uiConfig?: { [k: string]: any } | null | undefined;
  /**
   * Example configurations for this task
   */
  examples?: Array<{ [k: string]: any }> | undefined;
  /**
   * Whether task requires organization context (not available in personal mode)
   */
  requiresOrgContext: boolean;
  /**
   * Whether task requires event scope
   */
  requiresEventScope: boolean;
  /**
   * Task version
   */
  version: string;
};

/** @internal */
export const Category$inboundSchema: z.ZodMiniType<Category, unknown> =
  openEnums.inboundSchema(Category);

/** @internal */
export const TaskMetadata$inboundSchema: z.ZodMiniType<TaskMetadata, unknown> =
  z.pipe(
    z.object({
      task_type: types.string(),
      category: Category$inboundSchema,
      name: types.string(),
      description: types.string(),
      icon: z.optional(z.nullable(types.string())),
      tags: types.optional(z.array(types.string())),
      config_schema: z.record(z.string(), z.any()),
      ui_config: z.optional(z.nullable(z.record(z.string(), z.any()))),
      examples: types.optional(z.array(z.record(z.string(), z.any()))),
      requires_org_context: z._default(types.boolean(), false),
      requires_event_scope: z._default(types.boolean(), false),
      version: z._default(types.string(), "1.0.0"),
    }),
    z.transform((v) => {
      return remap$(v, {
        "task_type": "taskType",
        "config_schema": "configSchema",
        "ui_config": "uiConfig",
        "requires_org_context": "requiresOrgContext",
        "requires_event_scope": "requiresEventScope",
      });
    }),
  );

export function taskMetadataFromJSON(
  jsonString: string,
): SafeParseResult<TaskMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskMetadata' from JSON`,
  );
}
