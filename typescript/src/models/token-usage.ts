/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type TokenUsage = {
  /**
   * Input tokens consumed
   */
  input: number;
  /**
   * Output tokens generated
   */
  output: number;
  /**
   * Total tokens (input + output)
   */
  total: number;
};

/** @internal */
export const TokenUsage$inboundSchema: z.ZodMiniType<TokenUsage, unknown> = z
  .object({
    input: types.number(),
    output: types.number(),
    total: types.number(),
  });

export function tokenUsageFromJSON(
  jsonString: string,
): SafeParseResult<TokenUsage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TokenUsage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TokenUsage' from JSON`,
  );
}
