/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type TransferSpaceResponse = {
  /**
   * Transfer status
   */
  status: string;
  /**
   * ID of the transferred space
   */
  spaceId: number;
  /**
   * UUID of the target organization
   */
  targetOrgId: string;
};

/** @internal */
export const TransferSpaceResponse$inboundSchema: z.ZodMiniType<
  TransferSpaceResponse,
  unknown
> = z.pipe(
  z.object({
    status: types.string(),
    space_id: types.number(),
    target_org_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "space_id": "spaceId",
      "target_org_id": "targetOrgId",
    });
  }),
);

export function transferSpaceResponseFromJSON(
  jsonString: string,
): SafeParseResult<TransferSpaceResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TransferSpaceResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TransferSpaceResponse' from JSON`,
  );
}
