/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Response showing differences between two versions.
 */
export type VersionComparisonResponse = {
  spaceId: number;
  versionA: number;
  versionB: number;
  /**
   * Dictionary of field changes and relationship changes
   */
  differences: { [k: string]: any };
};

/** @internal */
export const VersionComparisonResponse$inboundSchema: z.ZodMiniType<
  VersionComparisonResponse,
  unknown
> = z.pipe(
  z.object({
    space_id: types.number(),
    version_a: types.number(),
    version_b: types.number(),
    differences: z.record(z.string(), z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "space_id": "spaceId",
      "version_a": "versionA",
      "version_b": "versionB",
    });
  }),
);

export function versionComparisonResponseFromJSON(
  jsonString: string,
): SafeParseResult<VersionComparisonResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VersionComparisonResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VersionComparisonResponse' from JSON`,
  );
}
