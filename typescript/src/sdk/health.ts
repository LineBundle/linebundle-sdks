/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthCheckAutomation } from "../funcs/health-check-automation.js";
import { healthCheck } from "../funcs/health-check.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Health extends ClientSDK {
  /**
   * Automation Health Check
   *
   * @remarks
   * Health check for automation system.
   *
   * Checks:
   * - API availability
   * - Prefect server connection
   * - Task registry
   * - Celery Beat (indirectly via task registry)
   */
  async checkAutomation(
    options?: RequestOptions,
  ): Promise<any> {
    return unwrapAsync(healthCheckAutomation(
      this,
      options,
    ));
  }

  /**
   * Health Check
   *
   * @remarks
   * Health endpoint.
   *
   * - Default (`/health` or `/health?shallow=true`) is **liveness**: fast, no external deps.
   * - Optional (`/health?deep=true` or `/health?shallow=false`) performs **readiness** checks.
   *
   * Readiness only treats DB + Redis as critical. Other checks are informational/skipped.
   */
  async check(
    request?: operations.HealthCheckHealthGetRequest | undefined,
    options?: RequestOptions,
  ): Promise<any> {
    return unwrapAsync(healthCheck(
      this,
      request,
      options,
    ));
  }
}
